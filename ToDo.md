# Installer
- [ ] komplett mit Docker-compose-files die im installer modifiziert werden können.
# Features
- [x] Architekten Interview
  - [x] feste Antworten (a, b, c) sollen per Butten gewählt werden können.
  - [x] das Architekten-Interview für spätere Referenz im Projekt ablegen.
- [x] Epochen-Erstellung (Interview)
- [x] Nicht-Netzwerk KI-Ziele (lokal und/oder Windows)
- [x] Persona bearbeiten
- [x] Verbotsliste bearbeiten
- [x] lokaler Speicherort für Geschichte definierbar
- [x] KI-Ziele mit Favorit-Haken (genau 1 Favorit möglich) erweitern. -> Wenn gesetzt, immer im Dropdown im Kopfbereich vorausgewählt und NICHT mehr "Lokal / Standard-Ollama".
- [x] integriere eine Readme Datei über einen Button im Kopfbereich. Inhalt vgl. Bedienungsanleitung.md unter /docs . Öffne schön formatiert in einem neuen Browser-Tab
- [x] Hilfe-System für Anwender (schreibe ein Hilfefile, dass 1. den Ablauf einer neuen Geschichte erklärt und 2. mögliche Fehler z.B.: Asynchronität behandelt den Umgang damit. Binde einen Button in die Kopfzeile ein, mit dem man den Text in einem eigenen Browser-Tab öffnen kann. Natürlich schön formatiert.)
- [x] Gebe im Fußbereich Auskunft, was die KI gerade macht, um dem Anwender sicherheit zu geben, dass nichts abgestürzt ist. Besonders beim prüfen des Kapitels ist da warten ohne Rückmeldung unangenehm aufgefallen.
- [x] Zeit-Überbrückung mit Benutzer-Unterhaltung (docs/unnützesWissen.csv in DB anlegen und in einem zentralen Overlay anzeigen, während die KI arbeitet). Beginne nach 20 Sekunden damit, wchsle alle 20 Sekunden die Nachricht und schliesse das Overlay, wenn die KI fertig ist.
- [x] Implementiere im Export-Tab auch einen PDF Export. Entwerfe hierfür auch ein entsprechendes PDF-Template, das etwas verspielt ist, wie ein hochwertiges Buch.
- [x] Füge im Speicherort (der Geschichten) die Möglichkeit ein (Checkbox), dass automatisch Unterordner für die Epoche und/oder [Name der Geschichte] angelegt und benutzt werden.
- [x] Lektorieren: Beide Seiten sollen dasselbe Text-Layout haben. Mit Zeilenwechseln, damit man weniger horizontal scrollen muss, aktuell wird das nur rechts umgesetzt.
  - [x] Füge eine Legende in die Kopfzeile ein mit den Farben für Grammatik, Rechtschreibung und Anachronismen. Und warum ist der scrollbar rechts viel breiter?
- [x] Wenn das Kapitel geschrieben ist, könnte man für die anderen Tabs die Infos gleich laden, sodass die Infos gleich da sind, wenn man das Tab wechselt. Das vermeidet auch, dass man versehentlich das flasche Kapitel nochmal lektoriert.
- [x] Epoche sollte auch auf Genre erweitert werden, da das fast fließend ineinander übergeht.
- [x] Quen3 LLM aus dem Test Kontext im Script nehmen. Sie steht gleichberechtigt neben Hermes3 als Schreiber.
- [x] Den Hauptbildschirm so einrichten, dass niemals gescrollt wird. Notfalls sollen die Container zu scrollen sein.
- [x] Architektenstrand soll zwischengespeichert werden können. Und dann auch fortgesetzt werden können.
- [x] Unnützes Wissen zufällig aus der DB holen und zur Überbrückung anzeigen.
- [x] KI-Integration, um während einer laufenden Geschichte Fragen zu stellen (zur Geschichte). Wünsche des Benutzers sollen gleich im nächsten Kapitel das geschrieben wird umgesetzt werden.
- [x] Neues Feature: Personen-Fundus; bereits in einer Geschichte benutze Personen (mit ihren Eigenschaften und die Namen der Geschichten in der sie benutzt wurden) sollen automatisch an einem Ort (md-File?) gespeichert werden. Bei einer neuen Geschichte fragt der Architekt, ob bekannte Personen in der aktuellen Geschichte benutzt werden sollen. Wenn ja, fragt der Architekt, nach einer Spezifikation der Figur (Name, Alter, Stand...). Claude schlägt nun alle passenden Figuren (je nach gewählter Epoche/Genre) vor und der Benutzer kann eine oder mehrere Figuren als Haupt- oder Neben-Figur auswählen. Diese müsste VOR der Frage nach Haupt und Nebenfiguren kommen, da dies eventuell diese Frage obsolet macht. Weiterhin soll der Fundus durch den User frei in einem neuen Tab bearbeitet werden können. Es soll auch eine Import-Funktion geben, dass benutzte Personen aus vorhandenen Geschichten ausliest und sie mit allen bekannten Eigenschaften importiert. Achte auf die Trennung nach Genre/Epoche! Am Anfang des Fundus' steht ein Template-Kommentar, wie eine neue Figur definiert sein könnte. Überlege dir ein Template, das du und der Benutzer gut lesen kann. (md-Syntax?).
- [x] KI-Konfiguration: Wenn es Änderungen auf dem KI-Server bezüglich der Verfügbaren KIs gibt, muss das von der Software erkannt werden. Es muss der Architekt benachrichtigt werden, damit er fehlende KIs nicht mehr abfrägt oder vorschlägt. Könnte man einen Prüflauf- in den Admin-Einstellungen einrichten, die das automatisch korrigiert?
- [x] Unnützes Wissen scheint nach meiner Wahrnehmung nicht zufällig zu sein. Bestimmte Fakten kommen recht oft.
- [x] Einblenden des unnützen Wissens konfigurierbar machen: einklappbarer Container "Benutzer-Einstellungen" in den Einstellungen (Platz für weitere Optionen), Toggle zum kompletten Ab-/Anschalten, bei "an" Startzeit in Minuten und Wechsel-Intervall in Sekunden einstellbar. Basiswerte = bisher festcodierte 20 s / 20 s. Persistenz in DB-Tabelle `einstellungen` (neue Spalten `unnuetzes_wissen_*`), Overlay liest die Werte bei jedem Wartezeit-Beginn frisch.
- [x] größeres Feature: Aus dem Geruest.md ein passendes Deckblattbild erstellen lassen. Welche KI wäre da gut und kann ich sowas lokal haben?.
  - Umgesetzt: sd-server (stable-diffusion.cpp, SDXL-Turbo, Vulkan) auf Athene, per Reverse-Tunnel an Prod (KI-Ziel "Athene", Bild-Port 18322). `cover_prompt`-Rolle fasst geruest.md zu einem englischen Bildprompt zusammen, `bild_generierung.py` erzeugt das Bild, wird als `cover.png` gespeichert und als PDF-Titelseite eingebunden. Alternativ: eigenes Titelbild hochladen.
  - [x] 2026-08-30: sd-server auf neueren Build umgestellt. `bild_generierung.py` von der OpenAI-kompatiblen API (`POST /v1/images/generations` + `<sd_cpp_extra_args>`-Prompt-Hack) auf die native async API umgebaut: `POST /sdcpp/v1/img_gen` → Job-ID → `GET /sdcpp/v1/jobs/{id}` pollen bis `completed`. Container brauchte zusätzlich `working_dir: /models` in der Compose-Datei, sonst scannte sd-server bei jeder Anfrage rekursiv ab `/` und starb an `/proc` (HTTP 500). End-to-End auf Prod verifiziert.
- [x] Prüfen & Anwenden zeigt alle (bis jetzt geschriebenen) Kapitel und die Fehler mit Kapitel und Zeile.
  - [x] Rechtschreibung macht das genauso. Dabei kann die Spalte Satzkontext entfallen. Auch das Textfeld für Ersatzwort entfällt. Ein Klick auf das Wort springt gleich zu der Stelle der Geschichte und dann direkt im Text ausgebessert werden.
  - [x] Der Kapitel-Spinner kann so in beiden Tabs entfallen.
- [x] Status in der Fusszeile aktualisieren. Es zeigt bereit, während Aktionen laufen (zum Beispiel im Auto-Modus). Es soll die aktuelle Aktion anzeigen. Animiere auch die Staus-Leute entsprechend.
- [x] Container allgemein einklappbar machen, wie Architekten-Gespräch-Container.
- [x] Recherchiere jede auf dem Produktiv-System vorhandene Epoche und erweitere die Autoren-Vorschrift, mit viel mehr Epochen-Infos. Lege besonders viel Wert auf das Verhalten zwischen Personen aus verschiedenen Ständen. einfacher Bauer zu Adeligem, Büger zu Ritter, Ritter zu Hochadel usw. Erweitere die Informationen zu Technologie, das tägliche Leben, Krankheiten, Religion, um in den Geschichten mehr Informationen zu haben, welche die Geschichte lebendiger machen.
- [x] Interview-Template zum offline ausfüllen. Kann man Importieren und der Interviewer fragt nur noch Dinge die dynamisch aus dem importierten Template entstehen oder wo er unsicher ist.
- [x] Prüfen: Mit fehlt der "Alles neu testen" Button im Tab "Prüfen und Anwenden" in Prod
- [x] Da die Schreibleistung immer besser wird, sollen bestehende Projekte/Geschichten (mit den nötigen Projektfiles) direkt per Icon im Projektlisten Eintrag neu geschrieben werden. Blende ein Popup ein, dass warnt das diese Aktion lang laufen wird, diese muss der User positiv beantworten. Abbrechen schliesst das Popup einfach. Der Schreiber soll dabei immer "Mistral" sein, egal was im Architekten-File steht. Technisch wird der bestehende Projekt-Server einfach dupliziert und im Namen um "_v2" erweitert. Im neuen Ordner werden alle Dateien gelöscht, die durch das Schreiben entstanden sind. Es soll so sein, als wäre der Architekt gerade abgeschlossen worden sein. Starte dann automatisch das Automatik-Schreiben.
- [x] Gerüst-Editor mit Struktur-Zwang (Vorfall: a-Blut-und-Ahornlaub-Die-Ehre-des-Verbotenen, 2026-08-20 - alle "Zielwortzahl"-Zeilen im Kapitelplan versehentlich entfernt, Automatik-Schreiber meldete faelschlich "keine Kapitel-Struktur"). Phase 1: serverseitige Validierung beim Speichern (`kapitelplan_pruefen()` in backend/app/core/geruest.py), meldet fehlende Zielwortzahl/doppelte Kapitelnummer mit klarer Fehlermeldung statt klaglos zu speichern. Phase 2: strukturierter Block-Editor NUR für den Kapitelplan-Abschnitt (`frontend/src/components/KapitelplanEditor.tsx` + `frontend/src/utils/kapitelplan.ts`) - eigene Karte pro Kapitel mit Formularfeldern (Titel, Ort, Anwesende Figuren, Ereignis, Zielwortzahl als Pflicht-Zahlenfeld, Funktion im Spannungsbogen, Stand der Liebeshandlung, Zustand am Kapitelende), "+ Kapitel"/"▲▼"/"Löschen", Client-seitige Pflichtfeld-Prüfung mit rot markierten Feldern VOR jedem Server-Roundtrip. Kapitelnummer ergibt sich automatisch aus der Array-Position, doppelte Nummern sind dadurch strukturell unmöglich. Bei "Neu schreiben" automatisch vorbefüllt, da der Editor beim Laden einfach den vorhandenen Kapitelplan parst. Rest des Gerüsts (Rahmen, Figuren, Ausgangslage, Regeln) bleibt bewusst Freitext in zwei Monaco-Editoren links/rechts davon, da diese Abschnitte je Epoche unterschiedliche, teils frei vom User erfundene Feldnamen haben (z.B. "Kaigyū" statt "Stand" bei einer selbst angelegten Epoche) - ein festes Schema dafür wäre ein eigenes, größeres Vorhaben (Epoche-Wizard umbauen). Offen geblieben: die enum-artigen Rahmen-Felder (Jugendschutz-Stufe, Autor-Modell, Automatische Fortsetzung) als echte Dropdowns rausziehen, da sie heute schon per Regex geparst werden.
- [x] Es soll jeder Epoche eine Farbe zugeordnet werden, um die Übersicht zu verbessern. Die Projektliste soll die entsprechende Farbe im Design wiedergeben, als farbiges Quadrat je Zeile (nicht in der Schriftfarbe). Umgesetzt über eine ".farbe"-Markerdatei je Epoche (gleiches Muster wie ".genre"), neuer Endpoint `PUT /api/epochen/{ordner}/farbe`, Farbwahl per natives Colorpicker-Widget im "Epoche bearbeiten"-Editor. Die 6 im Repo verwalteten Epochen (Mittelalter, Regency, Shadowrun, Zukunft, Altes-Aegypten, Jetzt-2026) sind mit Standardfarben vorbelegt; die 3 direkt auf Prod angelegten Epochen (Japanisches-Hochmittelalter, Deutschland-2026, Bayern-zur-Zeit-Koenig-Ludwigs-II.) noch per SSH nachzuziehen. Projektliste aktualisiert sofort (bestehendes `onEpochenGeaendert`-Callback-Muster).
- [x] Mouseover-Effekt (Rotation/Skalierung) im "Geschichten Erzähler"-Schriftzug entfernt (frontend/src/App.tsx).
- [x] Ein Schritt zurück im Architekten-Interview existierte bereits vollständig (Backend `verlauf_gekuerzt_ab()`, Frontend `bearbeiten()`/"✏️"-Button) - war aber unsichtbar (opacity-0, nur bei Hover über die eigene Chat-Blase sichtbar) und nirgends dokumentiert. Button jetzt dauerhaft sichtbar (leicht transparent, volle Deckkraft bei Hover) statt komplett unsichtbar, plus Erwähnung in Anleitung.md.
- [x] Mache die Haupt-Container als auch die einzelnen Kapitel-Container im Architekt einklappbar. Das gibt mehr Platz für den Block der gerade bearbeitet wird. Diese Art eine Geschichte zu definieren, soll als Wahl-Möglichkeit neben dem Architekten-Interview liegen.
  - [x] GeruestPage.tsx: die drei "Haupt-Container" innerhalb des äußeren "🗺️ geruest.md"-CollapsibleCard (Rahmen/Titel/Figuren/Konflikt/Nebenstrang, Kapitelplan, Ausgangslage/Offene Punkte/Regeln) sind jetzt einzeln einklappbar (neue `AbschnittKopf`-Kopfzeile, kein verschachteltes CollapsibleCard).
  - [x] KapitelplanEditor.tsx: jede Kapitel-Karte einzeln einklappbar (Zusammenfassung "Titel · Ort" bei eingeklappter Karte), plus "Alle einklappen"/"Alle ausklappen"-Toolbar ab 2 Kapiteln. Einklapp-Zustand wandert korrekt mit beim Verschieben/Löschen (haengt an Inhalt, nicht Array-Index); eine Karte mit neu gemeldetem Pflichtfeld-Fehler klappt sich automatisch wieder auf.
  - [x] Kapitel-Karten starten jetzt eingeklappt: beim ersten Befuellen des Kapitelplans (`KapitelplanEditor.tsx`, `initialEingeklappt`-Ref) und bei jedem erneuten Betreten des Tabs "Architekt / Gerüst" (neues `aktiv`-Prop von `App.tsx` -> `GeruestPage.tsx` -> `KapitelplanEditor.tsx`, `aktiv === "geruest"`) werden alle Karten eingeklappt (Karten mit Pflichtfeld-Fehler bleiben sichtbar offen). Der komplette Kapitelplan-Container bleibt zusaetzlich wie bisher eigenstaendig einklappbar.
  - [x] "Gerüst selbst schreiben" als gleichberechtigte Alternative zum Architekten-Interview: neue Auswahl im "✨ Neues Projekt anlegen"-Formular (ProjektePage.tsx). App.tsx entschied bisher rein danach, ob `projektDetail.geruest` gesetzt ist, ob das Architekten-Interview erzwungen wird - ein frisches Projekt hatte nie ein geruest.md und landete deshalb IMMER zwangsläufig im Interview. Bei "Gerüst selbst schreiben" wird direkt nach dem Anlegen ein Platzhalter-Skelett (neu: `frontend/src/utils/geruestVorlage.ts`, Struktur 1:1 nach `architekt.txt`s "## Ausgabe") gespeichert, wodurch App.tsx automatisch den Gerüst-Editor statt des Interviews zeigt - keine Änderung an App.tsx nötig. Per lokalem API-Test verifiziert (Feldererkennung: Jahr/Jugendschutz-Stufe/Autor-Modell/Automatische Fortsetzung korrekt, leerer Kapitelplan wird nicht als Fehler gewertet).
- [x] grösseres Feature als neuen Haupt-Tab: Analysator (powered by Mistral); Es soll möglich sein eine Geschichte/Novelle als Text zu importieren. Du als KI analysierst ihn und schreibst daraus ein komplettes Gerüst und alle weiteren Dateien, die notwendig sind, dass du die Geschichte als Projekt zum selbst neu schreiben verfügbar ist.
  - Neuer Tab "🔬 Analysator" (`frontend/src/pages/AnalysatorPage.tsx`): Text einfügen oder .txt/.md-Datei laden, Epoche wählen, "Analysieren" - legt sofort ein Projekt an (`pd.projekt_anlegen`, also inkl. Personas/Verbotsliste/Einleitungssatz der Epoche) und startet die Analyse als Hintergrund-Task, Fortschritt per Status-Polling wie beim Automatikmodus.
  - Backend: neue Rolle `"analysator"` (Mistral, wie explizit gefordert) in `rollen.py`, neue geteilte Persona `app/data/personas/analysator.txt`, neues Modul `app/core/analysator.py` (Text-Zerlegung in Kapitel per Ueberschriften-Erkennung oder Absatz-Fallback, max. 30 Kapitel, je Kapitel EIN Ollama-Aufruf -> `KapitelAnalyse`, EIN abschliessender Synthese-Aufruf fuer Rahmen/Titel/Figuren/Konflikt/Nebenstrang/Ausgangslage), neuer Router `app/api/analysator.py` (`POST /api/analysator/starten`, `GET /api/projects/{ordner}/analysator-status`). Kapitelplan und Regeln-Abschnitt werden bewusst NICHT vom Modell, sondern deterministisch zusammengebaut (Zielwortzahl = echte Wortzahl des Original-Abschnitts, keine Modell-Schaetzung). 24 neue Unit-Tests (`test_analysator.py`).
  - Live-Test-Erkenntnis (SSH-Ziel "Athene"): häufiges Status-Polling *während* eines laufenden Ollama-Aufrufs über den SSH-Tunnel verlangsamt die Antwort drastisch (20+ Min. statt ~1 Min. für ein kurzes Kapitel) - vermutlich GIL-Konkurrenz zwischen dem Tunnel-Forwarder-Thread (`ssh_manager.py`) und den vielen kurzen HTTP-Handlern. Poll-Intervall im Analysator-Tab deshalb bewusst auf 10s gesetzt (statt der 3-4s bei Automatik/Schreiben) - dasselbe Muster könnte auch dort relevant sein, noch nicht untersucht.
  - Zweiter Live-Fund: die Synthese übernahm das Rahmen-Format der Epoche-Persona teils wörtlich als Kommaliste statt eigener "Jugendschutz-Stufe: ..."/"Automatische Fortsetzung: ..."-Zeilen - ohne die noetigen woertlichen Marker waere eine harmlose Geschichte faelschlich als "voll explizit" eingestuft worden. Behoben mit doppelter Absicherung: verschärfte Prompt-Anweisung in der Persona PLUS deterministisches Nachbessern (`_rahmen_normalisieren()`) als zweites Sicherheitsnetz.
- [x] erweitere den Analysator. Es soll möglich sein aus der Geschichte eine neue Epoche abzuleiten. Schlage Angaben vor, diesse kann der Benutzer vor dem speichern noch edfitieren und ergänzen, falls Informationen fehlen. Wenn es sich um ein Franchise handelt, setze im EInleitungssatz der Epoche einen klaren Hinweis, dass der Schreiber nicht der Rigentümer an diesem Franchise hat, keine Rechte und dass dies als sogenannte FanFic (Fan Projekt) gesehen werden soll und keine Absicht von monetärer Nutzung besteht.
  - Im Analysator-Tab neue Wahl "📚 Bestehende Epoche verwenden" vs. "🧬 Neue Epoche aus dieser Geschichte ableiten". Bei Ableitung: neuer, dritter Analysator-Aufruf ("AUFGABE: EPOCHE-VORSCHLAG" in `analysator.txt`, JSON-Antwort ueber neues Schema `epoche_vorschlag_schema.py`) schlaegt aus einem kurzen Text-Auszug alle 13 Felder vor (Name, erfunden, Beschreibung, Zeitraum, Orte, Gesellschaft, Statusregel, Genre, Rang-Wort, Anreden, Nebenstrang-Typen, Vorbild-Franchise, Verbote) - bewusst EIN kurzer, synchroner Aufruf (keine Kapitel-Analyse noetig), damit der Vorschlag schnell kommt.
  - Formularfelder aus EpocheErstellenPage.tsx in neue geteilte Komponente `frontend/src/components/EpocheFormular.tsx` ausgelagert, damit derselbe editierbare Fragebogen fuer den KI-Vorschlag wiederverwendet werden kann statt eine zweite Kopie zu pflegen. Der Nutzer bearbeitet den Vorschlag inline, bevor "Analysieren" die Epoche ueber den bereits bestehenden Endpunkt `POST /api/epochen` anlegt und direkt danach die eigentliche Kapitel-Analyse mit der neuen Epoche startet.
  - FanFic-Hinweis: `app/core/epoche.py:einleitungssatz_vorlage()` haengt bei `erfunden=true` UND gesetztem `vorbild_franchise` automatisch einen Fan-Projekt-Hinweis (keine Rechte am Original, keine kommerzielle Nutzung) an denselben Satz an - sichtbar auf der Titelseite jeder damit geschriebenen Geschichte. Bewusst EIN durchgehender Satz statt eigener Absatz (titelseite_erzeugen() umschliesst den ganzen Einleitungssatz mit einem einzelnen Markdown-Kursiv-Sternchenpaar, das ueber eine Leerzeile hinweg nicht mehr erkannt wuerde). Server-seitiges Sicherheitsnetz: nennt die KI ein Franchise, aber vergisst `erfunden=true` zu setzen, korrigiert der Endpunkt das automatisch.
  - Live-verifiziert gegen Athene: ein synthetischer, klar an Star Wars angelehnter Test-Text wurde korrekt als "Star Wars"-Franchise erkannt (`vorbild_franchise` gesetzt, `erfunden: true`), die daraus angelegte Epoche bekam den FanFic-Hinweis korrekt im Einleitungssatz.
- [x] Komplette englische Übersetzung (inklusive /docs files) DIe README.md muss für GITHUB auch in englisch vorliegen.
  - Umfang mit dem User abgestimmt: nur README + /docs auf Englisch, die App-Oberfläche bleibt vorerst Deutsch (kein i18n-Umbau, eigenes größeres Vorhaben), Story-generierende Personas bleiben Deutsch ("Antworte auf Deutsch").
  - `README.md` ist jetzt die englische Fassung (GitHub-Konvention), `README.de.md` neu die deutsche - beide verlinken sich gegenseitig oben. Den "Stand dieser ersten Fassung"-Abschnitt dabei inhaltlich aufgefrischt (war veraltet: listete längst fertige Features wie das Architekten-Interview noch als "offen").
  - Alle vier `/docs`-Dateien (Anleitung, Bedienungsanleitung, Hilfe, Schnittstellen-Uebersicht) haben jetzt ein `*.en.md`-Geschwister mit vollständiger Übersetzung, gegenseitige Sprach-Links oben in beiden Fassungen. Deutsche Originale unverändert (werden weiter unveraendert von der - deutschen - laufenden App ausgeliefert). Feste UI-Beschriftungen (Buttons, Tab-Namen) bleiben im Englischen als deutsches Original stehen mit englischer Übersetzung in Klammern (z.B. "Anlegen" (Create)), damit man sie in der deutschen Oberfläche wiederfindet. Code-Bezeichner/Pfade in Schnittstellen-Uebersicht.en.md bewusst unübersetzt gelassen. Per 4 parallelen Fork-Agents mit gemeinsamem Glossar übersetzt, Struktur-Parität (Anzahl Überschriften) gegen die deutschen Originale verifiziert.
  - `docs/epochen-entwuerfe/*.md` und `docs/beispielbilder/` bewusst nicht übersetzt - interne Arbeitsnotizen des Users, keine Nutzer-Dokumentation.
# Bugs
- [x] eventuell funktioniert das fort schreiben der Tabs nicht richtig, ich hatte 2-mal Kapitel 3, kein Kapitel 4. Muss nochmal verifiziert werden.
- [x] Die Speicherort-Logik und der Ordnername mus nochmal verifiziert werden.
- [x] Epochen sollen bearbeitbar sein
- [x] Neu angelegte Epochen sollen sofort im Dropdown für neue Geschichten auftauchen.
- [x] Tab "Prüfen & Anwenden" zeigte nach einem Automatik-Lauf, der wegen zu vieler offener Stellen an den Nutzer uebergibt, noch den Stand vom letzten Besuch (Kapiteltexte/Befunde werden pro Kapitel nur GENAU EINMAL geladen, `geladenRef`). Fix: `App.tsx` zieht bei jedem Tab-Wechsel `projektDetail` neu; `PruefenAnwendenPage` bekommt ein `aktiv`-Prop und laedt beim Wechsel auf true den kompletten Stand frisch nach (`reloadToken`-Bump) - ausser es gibt noch nicht gespeicherte Editieraenderungen, die haben Vorrang (dann bewusst per "Neu laden" verwerfen).
- 

# Deployment
- [x] Implementiere einen User-Zugang mit Accounts. Benutze hierfür die bestehende DB. Hier im Entwickler-/Test-System kann der Login inaktiv sein, um es bequem zu halten. Aktiv ist es nur solange bis es erfolgreich implementiert und getestet ist.
  - [x] Schlage Lösungen vor, wie ein User NUR seine Projekte sehen kann, falls Geschichten auf dem KI-Server liegen bleiben.
- [x] Layout und Sicherheitskonzept für Frontend auf meiner Domain (daniel-ertl.de; hole Infos zum Deploy aus dem myReceipes Projekt) und KIs lokal auf meinem Server. Homepage->Router(Ports...)->lokaler Server(sowas wie ein API-Key?)